#!/bin/bash
#
# USAGE: qsub mpik-run-mage.qsub <mage-macro-file>
#
#$ -P short ### queue
#$ -j y     ### have logoutput and erroroutput in the same file
#$ -o /dev/null
#$ -e /dev/null

LOGFILE=$(echo $1 | cut -f 1 -d '.').out
cd "/lfs/l2/gerda/gerda-simulations/gerda-mage-sim"
singularity run --cleanenv --app MaGe ./UTILS/container/gerdasw.g4.10.3_v3.0.sqsh $1 &> $LOGFILE
